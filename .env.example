# ============================================================================
# Smart Stay Platform - Environment Variables Template
# ============================================================================
# 
# 使用方法:
# 1. このファイルを .env にコピー
# 2. 実際の値を設定
# 3. .env ファイルは絶対にコミットしないこと（.gitignoreで除外済み）
#
# 本番環境では、必ず環境変数を設定してください。
# デフォルト値は開発環境専用です。
# ============================================================================

# ============================================================================
# Database Configuration
# ============================================================================
POSTGRES_USER=smartstay
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=smartstay
DATABASE_URL=postgres://smartstay:your-secure-password-here@postgres:5432/smartstay?sslmode=disable

# ============================================================================
# JWT Configuration
# ============================================================================
# JWTトークンの署名に使用するシークレットキー
# 本番環境では、強力なランダムな文字列を設定してください
# 生成方法: openssl rand -base64 32
JWT_SECRET=your-very-secure-jwt-secret-key-here

# ============================================================================
# CORS Configuration
# ============================================================================
# フロントエンドのオリジン（開発環境）
CORS_ALLOWED_ORIGIN=http://localhost:3000

# ============================================================================
# Service Addresses (通常は変更不要)
# ============================================================================
AUTH_SVC_ADDR=auth-service:50051
RESERVATION_SVC_ADDR=reservation-service:50052
KEY_SVC_ADDR=key-service:50053

# ============================================================================
# Ports (通常は変更不要)
# ============================================================================
PORT=8080
